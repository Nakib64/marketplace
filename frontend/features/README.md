# Feature Modules Directory
This folder holds domain-driven feature slices (auth, jobs, proposals, contracts, chat, wallet).
Each feature slice contains its own:
- `api/`
- `hooks/`
- `schemas/`
- `types/`
- `components/`
- `utils/`
